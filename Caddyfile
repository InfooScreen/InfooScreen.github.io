infooscreen.dpdns.org {
	# basicauth {
	# 	<username> <hash>
	# }

	reverse_proxy website:8080

	@rendezvous {
		header Connection *Upgrade*
		header Upgrade websocket
	}

	reverse_proxy @rendezvous rendezvous:4000

	tls {
		ca https://acme-staging-v02.api.letsencrypt.org/directory
	}
}
