name: Bring-up stack for tests
on: push
jobs:
  e2e:
    runs-on: ubuntu-latest
    services:              # service containers live *inside* the job
      redis:               # example extra service
        image: redis:7-alpine
    steps:
      - uses: actions/checkout@v4
      - name: Compose up
        run: docker compose -d

